# model-context-protocol-todo-example

### Run python projects using 
```
poetry shell
poetry install
make run
```

### Run Nodejs project using 
```
npm install
npm run dev
```

Use the chat service at port 7000
Use TODO api service at 9001
MCP server will be running at 9002